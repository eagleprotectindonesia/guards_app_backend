{
  "name": "ep-guard-scheduling",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "postinstall": "turbo run prisma:generate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9",
    "prettier": "^3.3.3",
    "turbo": "^2.7.3",
    "typescript": "^5"
  }
}
